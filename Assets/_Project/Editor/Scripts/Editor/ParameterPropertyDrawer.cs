using UnityEditor;
using UnityEngine;

namespace PlaneWaver.Parameters
{
    public class ParameterPropertyDrawer : PropertyDrawer
    {
        //public override void OnGUI(Rect position, SerializedProperty property, GUIContent label) { base.OnGUI(position, property, label); }
    }
}